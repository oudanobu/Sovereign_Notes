apply plugin: 'com.android.application'

repositories {
    maven { url "https://maven.mozilla.org/maven2/" }
    google()
    maven { url "https://maven.aliyun.com/repository/google" }
    maven { url "https://maven.aliyun.com/repository/public" }
    mavenCentral()
}

android {
    compileSdkVersion 33
    buildToolsVersion "30.0.3"

    defaultConfig {
        applicationId "com.sovereignnote.core"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 5
        versionName "1.4.1"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        
        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a", "x86", "x86_64"
        }
    }

    flavorDimensions "engine"
    productFlavors {
        geckoview {
            dimension "engine"
            applicationIdSuffix ".geckoview"
        }
        systemwebview {
            dimension "engine"
            applicationIdSuffix ".systemwebview"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.2.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.0.4'
    
    // GeckoView 115 standard support (Multi-architecture)
    // Supports 64-bit ARM (modern devices like Xiaomi Android 13), 32-bit ARM (legacy),
    // and 32-bit Intel x86 (Nokia N1 native Intel Atom runtime support)
    geckoviewImplementation 'org.mozilla.geckoview:geckoview-arm64-v8a:115.0.20230710165010'
    geckoviewImplementation 'org.mozilla.geckoview:geckoview-armeabi-v7a:115.0.20230710165010'
    geckoviewImplementation 'org.mozilla.geckoview:geckoview-x86:115.0.20230710165010'
    geckoviewImplementation 'org.mozilla.geckoview:geckoview-x86_64:115.0.20230710165010'
}
